# Compatibility launcher

This directory preserves the former `python bd_colors_fast/app.py` command for
local shell helpers and other automation. The launcher delegates to
`mocaviz.app`; maintained code, data, and static assets belong in `mocaviz/`.
